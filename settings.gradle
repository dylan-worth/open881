rootProject.name = 'open881'

dependencyResolutionManagement {
    repositories {
        mavenCentral()
        flatDir {
            dirs 'server/libs'
        }
    }
}

include(
    'client',
    'server'
)
